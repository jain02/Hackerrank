#include<stdio.h>
int main()
{
	int n,arr[1000],count=0,arr2[10000],i,min,j,a=0;
	printf("Enter the size of Array: ");
	scanf("%d",&n);
	printf("Enter the Element of Array: ");
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);	
	}
	for(i=0;i<n;i++)
	{
		if(arr[i]!=0)
		{
			count++;
		}
	}
	printf("%d\n",count);
	count=0;
	while(n!=0)
	{
		min=arr[0];
		for(i=0;i<n;i++)
		{
			if(min>arr[i])
			{
				min=arr[i];	
			}	
		}
		printf("min:%d\n",min);
		for(i=0;i<n;i++)
		{
			if(arr[i]==0)
			{
				continue;
			}
			else
			{
				arr[i]-=min;
				printf("a:%d\n",arr[i]);
			}
		}
		for(i=0;i<n;i++)
		{
			if(arr[i]!=0)
			{
				count++;
			}
		}
		if(count!=0)
		{
			printf("%d\n",count);
		}
		else
		{
			n=0;
		}
		count=0;
		for(i=0;i<n;i++)
		{
			if(arr[i]==0)
			{
				a++;	
			}
			else
			{
				arr2[i-a]=arr[i];
				printf("Arr:%d\n",arr2[i-a]);
				count++;
			}
		}
		for(i=0;i<count;i++)
		{
			arr[i]=arr2[i];
			printf("A1:%d\n",arr[i]);
		}
		n=count;a=0;count=0;	
		printf("n:%d\n",n);
	}
}
